// Fichier : backend-proxy/package.json
{
  "name": "discord-social-graph-proxy",
  "version": "1.0.0",
  "description": "Proxy pour gérer l'authentification Discord OAuth2 et le stockage des données.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js" 
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7",
    "cors": "^2.8.5",
    "mongodb": "^6.3.0"  <-- NOUVEAU
  }
}
